#!/bin/sh
set -e
if [ "$1" = "remove" ] || [ "$1" = "purge" ]; then
  rm -f /etc/default/grub.d/90-naabiga.cfg
  update-alternatives --remove default.plymouth /usr/share/plymouth/themes/naabiga/naabiga.plymouth 2>/dev/null || true
fi
exit 0
